# task-3-web-development
A simple REST API for managing a list of books using Node.js, Express, and in-memory storage (no database).

This project demonstrates basic CRUD operations:

Create a book (POST)

Read all books or a single book (GET)

Update a book (PUT)

Delete a book (DELETE)
